GITHUB_TOKEN=
# GitHub personal access token used to fetch repository data.
# Create one from: GitHub → Settings → Developer settings → Personal access tokens.
# Public repos work with a fine-grained or classic token (read-only is enough).

NODE_ENV=
# Runtime environment.
# Use "development" for local development and "production" for deployments.

OPENROUTER_API_KEY=
# API key for OpenRouter to access LLMs.
# Get it from https://openrouter.ai after signing in.

OPENROUTER_MODEL=
# Model identifier to use via OpenRouter.
# Example: mistralai/devstral-2512:free
